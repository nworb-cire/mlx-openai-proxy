# Agent Notes

- After making a major change or bugfix, restart the local service stack so the running proxy reflects the new code before handing work back.
- If restart fails, report the failed command and the relevant log output.
